
- `JWT_SECRET` — Секретний ключ для підпису та перевірки JWT-токенів (мінімум 32 символи).
- `DB_USERNAME` — Логін для підключення до бази даних PostgreSQL.
- `DB_PASSWORD` — Пароль для підключення до вашої локальної бази даних PostgreSQL.